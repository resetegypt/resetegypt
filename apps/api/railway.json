{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile=false && pnpm --filter @reset/api exec prisma generate && pnpm --filter @reset/api build"
  },
  "deploy": {
    "startCommand": "cd ../.. && pnpm --filter @reset/api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
